RSpec.describe Rspec::Parameterized::Context do
  it "has a version number" do
    expect(Rspec::Parameterized::Context::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
